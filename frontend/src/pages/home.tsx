import React from "react";
import Button from "../components/button";

function Home() {
  return (
    <div>
      <Button text="Teste" color="primary" onClick={() => {}} />
    </div>
  )
}

export default Home